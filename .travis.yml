---
language: python
python: 3.6
sudo: required

services:
  - docker
install:
  - pip install pipenv
  - pipenv sync
script:
  - pipenv run molecule test

notifications:
  webhooks: https://galaxy.ansible.com/api/v1/notifications/
